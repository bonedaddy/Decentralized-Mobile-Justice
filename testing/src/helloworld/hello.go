package main

// main tells the go-tool to create an executable command instead of a packge used by other go programs
import (
	"fmt"
	"src/stringUtil"
)

func main() {
	fmt.Println("hello")
	a, b, c := true, false, false
}
echo 
